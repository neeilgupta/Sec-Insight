services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./chroma_db:/app/chroma_db
    env_file: .env
    environment:
      - CHROMA_PERSIST_DIR=/app/chroma_db
